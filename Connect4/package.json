{
  "name": "connect4-fullstack",
  "version": "1.0.0",
  "private": true,
  "description": "Connect 4 - React + TypeScript + Tailwind v4 client, Node + Socket.IO server, shared game engine",
  "type": "module",
  "engines": {
    "node": ">=22.22.0"
  },
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "npm run build -w shared && concurrently -k -n shared,server,client -c gray,blue,green \"npm run dev -w shared\" \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "start": "npm run start -w server",
    "test": "npm run build -w shared && npm run test -w shared && npm run test -w server && npm run test -w client",
    "typecheck": "npm run build -w shared && npm run typecheck -w server && npm run typecheck -w client"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "typescript": "^7.0.2"
  }
}
