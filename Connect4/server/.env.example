# Port the API + WebSocket server listens on
PORT=3001

# Origin(s) allowed to call the server from a browser (comma separated).
# Only matters when the client is served from a different origin than the server,
# e.g. Vite dev server on :5173 talking to the API directly.
CLIENT_ORIGIN=http://localhost:5173
